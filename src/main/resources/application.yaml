spring:
    application:
        name: VkGrpcCRUD
    data:
        tarantool:
            host: "localhost"
            port: 3301
            username: "admin"
            password: "password"

grpc:
    server:
        port: 9090
